# zero
# one
# not

dup {


  Dup in terms of a loop
    Unfortunately zero not and one are defined
    in terms of dup


  ,$one,           Control bit

  if 1 [
    $one $one      Result
    ,$not,         Flip control bit
    $zero          End loop
  ], else [
    ,$zero $zero,   Result
    $zero           End loop
  ],

}
